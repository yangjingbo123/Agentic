llm:
  model_path: "/home/nvidia/jingbo/models/Qwen3-8B"

data:
  train_path: "data/math_train_rl.jsonl"
  test_path: "data/math_test_clean.jsonl"

agentic:
  epochs: 3
  batch_size: 16
  n_samples: 4
  lr: 1.0e-6
  clip_epsilon: 0.02
  max_tokens: 512
  max_rounds: 2
  max_interactions: 2
  kl_coef: 0.1
  target_kl: 6.0
  max_grad_norm: 1.0
